{
  "name": "probe-username-osint",
  "version": "1.0.0",
  "description": "OSINT username search across 373 functional sources",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:assets": "node build-assets.js",
    "check": "node --check server.js && node --check script.js && node --check worker.js && node --check build-assets.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "terser": "^5.51.2"
  },
  "dependencies": {
    "cheerio": "1.2.0"
  }
}
