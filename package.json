{
  "name": "probe-username-osint",
  "version": "1.0.0",
  "description": "OSINT username search across 100+ platforms",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check worker.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {},
  "dependencies": {
    "cheerio": "1.2.0"
  }
}
