services:
  - type: web
    name: probe
    runtime: node
    buildCommand: npm install && node_modules/.bin/playwright install chromium
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
